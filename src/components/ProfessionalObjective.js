import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faInfoCircle } from "@fortawesome/free-solid-svg-icons";

const ProfessionalObjective = () => {
  return (
    <section>
      <h2>
        <FontAwesomeIcon icon={faInfoCircle} /> Professional Objective
      </h2>
      <p>
        Motivated Junior Web Developer with experience in React, Node.js, HTML,
        CSS, and JavaScript. Seeking a remote position to apply and grow my
        skills in a dynamic company.
      </p>
    </section>
  );
};

export default ProfessionalObjective;
